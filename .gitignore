# Secrets and local environment
.env
.env.*
*.env
!.env.example
!.env.laptop.example
!.env.laptop-gpu.example

# Private teaching / presentation material — must never land in the public repo.
# The workshop proposal and presentation source live in a separate private repo.
2026-03-10 Angebot KI-Workshop v2.0.docx
*.docx
teaching/
presentation/

# Local model and data caches
hf-cache/
*.cache/
qdrant_storage/
open-webui-data/

# Local session handoff (team-internal, not part of the public deliverable)
HANDOFF.md

# Design contracts: the durable record (spec.md + plan.md) under
# contracts/<date-slug>/ is tracked as the public design record. The
# handoff/session scaffolding under handoff-trees/ is team-internal working
# state — same category as the root HANDOFF.md above — and stays local, not part
# of the public deliverable.
contracts/*/handoff-trees/

# Editor / OS noise
.DS_Store
*.swp
